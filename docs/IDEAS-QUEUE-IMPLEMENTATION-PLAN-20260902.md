# خطة تنفيذ بقية أفكار MindsHub/DSH — 2026-09-02 (تصميم قراءةً-فقط، 9 وكلاء)

**قاعدة المالك**: لا جولة مدفوعة قبل استكمال الطابور ومتابعات اقتصاد التوكنز؛ التجربة محلياً أولاً.

## الترتيب (الحاكم)

1. 1. S13.0-b plugins.trailCompaction (cost — the measured 36k+/call growth; −48% raw modelled)
2. 2. IDEA 10 plugins.turnBudget + ABDO_TURN_TOKEN_CAP (cost — hard stop on the runaway turn class measured tonight; lands the `const charged =` refactor at cli.ts:889 that IDEA 4 must build on)
3. 3. IDEA 4 routerGate front gate (cost — removes whole loops for trivial chat turns; default off; its gate call MUST charge the turn meter from #2)
4. — OWNER GATE: after 1–3 are green + dry-run receipts recorded, one paid ON/OFF round on the same objective may be approved —
5. 4. Registry sprint = IDEA 5 (WP1+WP2) MERGED with IDEA 12 inventory into ONE plugin-registry.ts, one reader API, one frame field; converts all plugin reads at once (7 legacy + trailCompaction + turnBudget); IDEA 11 `{when}` DSL split off as optional WP-B pending owner decision (see conflicts)
6. 5. IDEA 3 remainder plugins.receiptFixtures (quality — its tap is what makes future local replays faithful; land before more text-behaviour changes)
7. 6. IDEA 2 plugins.intentField (quality — default OFF; rebases onto S13.0-b's followUp/dispatch changes in text-agent-loop.ts)
8. 7. IDEAS 7+8+9 approvalTakeover / trajectory / deliverables (ui — largest estimate, pure shell, registers descriptors instead of static rows once #4 has landed)

## المنطق
Owner rule: cost-relevant first, no paid round before the remaining ideas land and are dry-run. Verified in the frozen tree (rust-main d1209e4) that the cost problem is exactly where S13.0-b locates it: only read-class entries are tracked for compaction (text-agent-loop.ts:306 `readEntries`, :313-330 `maybeCompactReads`, :451 `isReadCommand ? command : undefined`) while every result is appended at 14_000 chars (:449); so trailCompaction is the direct fix and goes first. turnBudget goes second because it stops the failure class itself (a runaway turn) rather than saving per-call, and because it owns the refactor of the single ledger write site (cli.ts:886-890 → `const charged = chargeableUsage(...)`) that the front gate's second ledger site must copy and also charge into. routerGate third: it saves whole loops but only on chat-lane trivia (not tonight's driver) and defaults off. The three cost specs touch disjoint cli.ts regions except :889 and the desktop reset literal, so they merge cheaply when done in this order. The registry (IDEA 5) and inventory/rules (IDEA 12+11) both create `packages/engine/src/plugin-registry.ts` with incompatible APIs and both take over the same seven read sites with different literals and both assert zero remaining `loadSettings().plugins?.` reads — they cannot land separately; merged into one sprint placed AFTER the cost trio so it converts ten reads once (the cost specs use the legacy idiom, verified as the current pattern at cli.ts:889/2381/2403/2502/2506/2512/2618) and so every later toggle (5,6,7) becomes a descriptor instead of a hand-written index.html row + serve-wiring re-pin. receiptFixtures before intentField because its tap captures full run receipts (today every persisted form is a 500-char head slice — verified cli.ts:2560/2646), which is the only way the dry-runs for later text-behaviour ideas can replay real receipts instead of synthetic sizes. intentField last of the engine changes because it edits the same text-agent-loop.ts followUp/dispatch lines S13.0-b rewrites and adds ~20 tokens/call. UI trio last: 3 implementer-days, no cost effect, and it benefits most from the registry (rows, inventory of approval.js/trajectory.js actually loaded).

## التعارضات والمقاعد المشتركة (يجب احترامها عند التنفيذ المتتابع)

- DUPLICATE MODULE: IDEA 5 and IDEA 12+11 both create packages/engine/src/plugin-registry.ts — IDEA 5 exports PLUGINS (descriptor array) + resolvePlugins/validatePluginsPatch/describePlugins + env pins ABDO_PLUGIN_* + pluginsRevision fence + `pluginOn(name)` readers; IDEA 12 exports PLUGIN_TOGGLES (object) + parseRule/evaluateRule + PluginInventory + `plugins.read(name, site, epoch)` readers + a `plugins` out frame. Both rewrite the same seven reads (cli.ts:889, 2381, 2403, 2502, 2506, 2512, 2618) with DIFFERENT literals and both re-pin token-economy-wiring.test.ts:13/:27 and serve-wiring.test.ts:107/:173 to their own strings. Resolution: one module, descriptor array (IDEA 5 shape) + inventory recording (IDEA 12 `read(name, site)` API, since site/epoch bookkeeping is the inventory's value); one frame field name (IDEA 5 says `pluginRegistry`, IDEA 12 says `pluginCatalog` — pick one); one framed-stdio.test.ts extension after :89 (both specs extend the same child).
- VALIDATOR CONTRADICTION: IDEA 5 validatePluginsPatch refuses any non-boolean plugin value (`plugins.<k> يحتاج true أو false`); IDEA 11 stores `{ when: "rail == thin" }` objects under the same keys and changes Settings.plugins type at cli.ts:607. Cannot both be true. Resolution: WP-A boolean-only registry (IDEA 5 semantics, fail-closed); IDEA 11 DSL as a separate owner decision (WP-B) that widens the validator and the type in one place — not folded silently.
- SINGLE LEDGER LINE cli.ts:889 (verified: `...chargeableUsage(decoded.usage, estimated, requestOutputCap, loadSettings().plugins?.cacheAccounting !== false),` inside `recordCloudUsage({` count 1, pinned exactly by token-economy-wiring.test.ts:13 and :16): IDEA 10 rewrites it to `const charged = chargeableUsage(...)`; IDEA 4 adds a SECOND `recordCloudUsage({` with `...chargeableUsage(decoded.usage, estimated, GATE_OUTPUT_TOKENS, ...)` and `note: "router.gate"` (pin :16 → 2); registry sprint rewrites the `loadSettings().plugins?.cacheAccounting !== false` argument. Three specs, one line, three re-pins. Land in that order (10 → 4 → registry); each re-pins the literal honestly.
- IDEA 10 test (4) `source.match(/chargeableUsage\(decoded\.usage/gu)).toHaveLength(1)` is broken by IDEA 4's gate site, which also calls `chargeableUsage(decoded.usage, ...)`. Resolution: when IDEA 4 lands, that pin becomes length 2 (or the gate decodes into a differently named variable) — and, more importantly, IDEA 4's gateAsk MUST call `hooks.turnMeter?.charge(charged)` for its own cloud call: IDEA 4's text says idea 10 'would later compose by counting the gate entry via its ledger note', but the TurnSpendMeter is charged by explicit charge() calls, not by ledger notes — without the call, gate spend escapes the per-turn cap.
- DESKTOP RESET LITERAL index.html:1341 (verified: `plugins: { swarm: false, reviewer: false, activity: true, walls: true, verifier: false, toolVerdict: true, miner: true, readCompaction: true, cacheAccounting: true, resumeIntent: true }`) pinned twice by serve-wiring.test.ts:95 and :205 (`cacheAccounting: true, resumeIntent: true }`): S13.0-b inserts `trailCompaction: true,` after `toolVerdict: true,` (keeps the tail pin); IDEA 10 appends `turnBudget: true`; IDEA 2 appends `intentField: false`; IDEA 3-rem appends `receiptFixtures: true`; IDEA 7-9 append three keys; IDEA 12 appends `inventory: true, rules: true`; IDEA 5 DELETES the static rows :313-322 and computes the reset from descriptors (pin :95 inverted to not.toContain). Seven specs on one string. Resolution: cost trio (1–3) each re-pin the tail once; the registry sprint (4) removes the literal and the static rows for good; 5–7 add descriptors only and touch neither index.html rows nor these pins.
- ZERO-LEGACY-READ ASSERTIONS: IDEA 5 WP2 `expect(source).not.toMatch(/loadSettings\(\)\.plugins\?\./)` and IDEA 12 'ZERO occurrences of `loadSettings().plugins?.`' are violated by every spec that lands after them with the legacy idiom (S13.0-b `plugins?.trailCompaction === false`, IDEA 10 `plugins?.turnBudget !== false`, IDEA 2 `plugins?.intentField === true`, IDEA 3 `plugins?.receiptFixtures !== false`, IDEA 7 gate() `plugins?.approvalTakeover`, IDEA 9 `plugins?.deliverables`). Resolution: cost trio lands BEFORE the registry using the legacy idiom (current pattern), the registry sprint converts all ten reads and adds descriptors for trailCompaction/turnBudget; specs 5–7 must use the registry reader from the start and drop their `loadSettings().plugins?.` lines. IDEA 5 T2c parity scan (`plugins\?\.(\w+)` ⊆ PLUGIN_NAMES) and IDEA 12's hardcoded '12 rows' / '11 entries' counts must become descriptor-count-driven, not literals.
- text-agent-loop.ts followUp (:331-352, verified signature `followUp(prompt, readCommand?)`) and dispatch site (:431-451, verified `options.onTool?.(command)` / `options.onToolResult?.(command, output, verdict, idempotencyKey, mutated)` / `isReadCommand ? command : undefined` at :451): S13.0-b changes followUp's second arg to `{command, kind, verdictLine}` and generalises readEntries→trailEntries; IDEA 2 adds trailing `intent` to onTool/onToolResult (:33-34, :431-433), splits the intent line before parseCommand, and swaps the followUp instruction text at :452 ON-only. Same lines, different concerns. Resolution: S13.0-b first (cost); IDEA 2 rebases; both add describes to the CRLF engine-host test file and exports to engine-host/src/index.ts (CRLF) — merge, do not duplicate.
- cli.ts onToolResult closure (:2622-2646, verified 5-arg signature pinned by serve-wiring.test.ts:113): IDEA 2 changes it to 6 args (re-pins :113); IDEA 3-rem inserts `tap?.observe(cmd, output, verdict, epoch)` after `allReceipts.push` (:2624) and in the acceptance path after :2552; IDEA 9 appends `...(deliverablesOn ? { locations: locationsFromReceipt(...) } : {})` to both `tool-result` emits (:2560, :2646). Three insertions in one closure; order 3-rem → intentField → deliverables, each preserving the others' pinned substrings.
- Event-line cluster cli.ts:2675-2683 (verified: ✓ checkpoint :2675, 📐 :2677, 💳 :2681-2682, stuck block :2683): S13.0-b appends ` · ضغط التنفيذ=… · أثر الحقبة=…` to :2675 after the pinned `ضغط القراءة=${loop.readCompactions}`; IDEA 2 inserts the 🎯 emit after the 📐 line; IDEA 10 inserts ⏱ + `if (s.tripped)` block after 💳 and before :2683 with an index-order pin against `⚠ رد النموذج بلا أداة` (:2704). Compatible if each uses its own anchor; do not reorder.
- AskHooks (cli.ts:563-566, verified {onDelta, signal}) and the hooks object (:2395): IDEA 10 adds `turnMeter?` + `...(turnMeter === undefined ? {} : { turnMeter })`; IDEA 2 adds `intentField?: true` + `...(intentOn ? { intentField: true } : {})`. Both pin their own spread substring — compatible; IDEA 2 must not reformat the hooks literal.
- Settings type :596-612 / SETTINGS_KEYS :614 / validateSettingsPatch :635-668: IDEA 4 adds flat keys routerGate+gateModel (to SETTINGS_KEYS, ref-loop :643, enum check :654); IDEA 5 adds `pluginsRevision` to the type but NOT to SETTINGS_KEYS (write-protected) and a `plugins` branch in the validator; IDEA 12 widens the plugins value type. Same function, different branches — mergeable, but IDEA 4's routerGate is a tri-state flat key outside the registry (IDEA 5 T2c scan does not cover it) — note it in the registry sprint as 'flat setting, validated by hand'.
- shell-protocol.ts (LF): IDEA 5 edits :41 (settings-set allowed += expectedPluginsRevision) and :112-113; IDEA 12 adds a `plugins` out row after :69 and a transport-codegen pin. Different lines, same file, same sprint after the merge — one commit.
- IDEA 9 wires the dead `plugins.activity` checkbox (index.html:315) to hide #activity; IDEA 5/12 descriptors mark activity `wired:false` / site 'panel'. After IDEA 9 lands the descriptor must flip to wired:true — otherwise the inventory lies in the opposite direction.
- IDEA 10 and IDEA 4 both call selectTurnModel/loadSettings near the turn start and both add fields to the `active_task` fact (`turn_budget` stopReason vs `gate-answered` status); IDEA 4 returns before lastStop exists, so no lastStop-union clash — but resume-intent.ts pickPriorGoal must skip both `answered` (IDEA 4) and, if the owner wants, `turn_budget`-checkpointed goals should still be resumable (they should: turn_budget = checkpointed, not answered).
- Every CRLF file is shared: cli.ts, index.html, engine-host/src/index.ts, tool-verdict.ts, engine-host/test/text-agent-loop.test.ts, turn-memory.ts, failure-tiering.ts, project-sprint-plan-guard.ts, docs/PRODUCT-CLOSURE-10-SPRINT-PROGRAM.md (all measured w/crlf this session via `git ls-files --eol`; index is LF, core.autocrlf=true). A single LF rewrite by any of the seven sprints turns the next sprint's rebase into a whole-file conflict.

- مقعد: packages/engine/src/cli.ts:889 — single chargeableUsage/recordCloudUsage site (specs: IDEA 10, IDEA 4, registry) + token-economy-wiring.test.ts:13/:16
- مقعد: packages/engine/src/cli.ts:2618 — readCompaction spread (S13.0-b adds sibling spread; registry rewrites the read) + token-economy-wiring.test.ts:27 and `runTextAgentLoop({` count-1 pin
- مقعد: packages/engine/src/cli.ts:2381/2403/2502/2506/2512 — the per-turn toggle reads (registry rewrites all; serve-wiring.test.ts:107/:173 pins)
- مقعد: packages/engine/src/cli.ts:2374-2432 — turn start: IDEA 12 currentPlugins after :2374, IDEA 10 meter before hooks :2395, IDEA 2 intentOn beside :2396-2399, IDEA 4 gate between :2421 and :2432
- مقعد: packages/engine/src/cli.ts:2497 — lastStop union (IDEA 10 adds "turn_budget", reused from contracts/src/session.ts:62 — verified present)
- مقعد: packages/engine/src/cli.ts:2545-2562 acceptance-probe receipt path (IDEA 3 tap after :2552; IDEA 9 locations at :2560)
- مقعد: packages/engine/src/cli.ts:2622-2646 onToolResult closure (IDEA 2 signature, IDEA 3 tap, IDEA 9 locations) + serve-wiring.test.ts:113 pin
- مقعد: packages/engine/src/cli.ts:2675-2683 checkpoint/📐/💳/stuck event-line cluster (S13.0-b, IDEA 2, IDEA 10)
- مقعد: packages/engine/src/cli.ts:563-566 AskHooks + :2395 hooks literal (IDEA 10 turnMeter, IDEA 2 intentField)
- مقعد: packages/engine/src/cli.ts:596-614 Settings type + SETTINGS_KEYS, :635-668 validateSettingsPatch (IDEA 4 flat keys, IDEA 5 pluginsRevision + plugins branch, IDEA 12 value type)
- مقعد: packages/engine/src/cli.ts:2018/2219/2252 ready + settings frames (IDEA 5 pluginRegistry vs IDEA 12 pluginCatalog — one name)
- مقعد: packages/engine/src/cli.ts:2910-2917 completion `.then` (IDEA 12 `plugins` frame before done; IDEA 9 relies on turnServers.stopAll at :2912 preceding done — verified)
- مقعد: packages/engine/src/plugin-registry.ts (NEW) — claimed by both IDEA 5 and IDEA 12+11; must be one module
- مقعد: packages/engine-host/src/text-agent-loop.ts:57-60 options/ReadTrailEntry, :306-352 readEntries/maybeCompactReads/followUp, :431-451 dispatch site (S13.0-b, IDEA 2) — LF
- مقعد: packages/engine-host/src/index.ts:148-171 exports (S13.0-b compactTrail/isExecDigest/verdictLineOf; IDEA 2 splitIntent/INTENT_PREFIX) — CRLF
- مقعد: packages/engine-host/test/text-agent-loop.test.ts (S13.0-b + IDEA 2 new describes) — CRLF
- مقعد: packages/desktop/ui/index.html:310-323 plugins panel rows + :1341 reset literal + :1193/:1312 generic loops (S13.0-b, IDEA 10, IDEA 4 select row, IDEA 5 deletes rows, IDEA 12 badges, IDEA 2, IDEA 3, IDEA 7-9) + serve-wiring.test.ts:93-95/:203-205 pins — CRLF
- مقعد: packages/engine/test/serve-wiring.test.ts (LF) — re-pinned by every spec; packages/engine/test/token-economy-wiring.test.ts (LF) — by S13.0-b, IDEA 10, IDEA 4, registry; packages/engine/test/framed-stdio.test.ts:89 (LF) — extended by IDEA 5 and IDEA 12 in the same child
- مقعد: packages/transport-contracts/src/shell-protocol.ts:41/:69/:112-113 (IDEA 5 inbound field, IDEA 12 out frame) — LF
- مقعد: docs/PRODUCT-CLOSURE-10-SPRINT-PROGRAM.md:250-259 (CRLF) and docs/IDEAS-INVENTORY-MINDSHUB-DSH-20260901.md:30-73 (LF) — every spec writes its toggle row / strikes its queue item
- مقعد: Sole-owner files (no seam): engine/src/turn-budget.ts + project-sprint-plan-guard.ts (IDEA 10); engine/src/front-gate.ts + resume-intent.ts (IDEA 4); engine/src/receipt-fixtures.ts + secret-command-guard.ts + .gitignore + test/fixtures/ (IDEA 3); engine-host/src/intent-line.ts + engine/src/intent-field.ts + native-agent-tools.ts (IDEA 2); approval-ledger.ts, tool-locations.ts, turn-memory.ts exports, shells/*.ts, desktop/scripts/prepare.ts (IDEA 7-9)

## خطة التجربة المحلية (بلا رصيد)
GLOBAL PRECONDITIONS (every sprint): (0) live round shows its `done` frame and no bun/abdo-tool-worker process has <جذر المستودع> as cwd; work in a fresh worktree branched from d1209e4 (AGENTS § التوازي rule 1); re-measure EOL per file with `git ls-files --eol <path>` immediately before the first edit (this session: cli.ts/index.html/engine-host index.ts/tool-verdict.ts/text-agent-loop.test.ts/turn-memory.ts/failure-tiering.ts/project-sprint-plan-guard.ts/PRODUCT-CLOSURE doc = CRLF; text-agent-loop.ts/token-budget.ts/resume-intent.ts/secret-command-guard.ts/native-agent-tools.ts/shell-protocol.ts/prepare.ts/all engine tests/.gitignore/IDEAS inventory = LF); Edit tool only, never heredoc or `git add -A`. (1) Gates per sprint: `bun run test` inside packages/engine-host and packages/engine (and packages/transport-contracts, packages/providers when touched) — never root `bun test` (memory abdo-kernel-30); `bun turbo typecheck` at root. (2) BASELINE ONCE, before sprint 1: with ABDO_CODE_SETTINGS pointing at a scratch settings file (local ollama chat+agent models, all plugin defaults), run the framed engine (pattern scripts/agent-project-e2e.ts:95-175 / framed-stdio.test.ts:15-26) against a throwaway project with a fixed script of bodies — «مرحبا», a real agent task with a deliberately failing `npm test`, «ماذا فعلت؟», «اكمل» — and save the frame stream and the model-facing epoch input (ABDO_DEBUG capture). Every later sprint replays the same script with its new toggle OFF and diffs against this baseline minus seq/timestamps: OFF = byte-identical is then proven globally, not just per unit test. HONESTY NOTE: tonight's real receipts are not recoverable (every persisted form is a 500/700/1500-char head slice — cli.ts:2560/2646/2669), so the S13.0-b replay is synthetic-from-measured-sizes; IDEA 3's tap is what makes future replays real. SPRINT 1 (trailCompaction): engine-host tests (g)-(o) incl. the 32-round replay (10 runs×14k + 20 reads×6k + 2 writes with shipped READ_COMPACTION/TRAIL_COMPACTION parsed from cli.ts) asserting ≥40% fewer summed history chars vs readCompaction-only, prefixBreaks ≤6, newest 2 exec results full-size; record its printed totals in PRODUCT-CLOSURE-10-SPRINT-PROGRAM.md as the S13.0-b local receipt; then one local 9B agent turn ON vs OFF: OFF frame stream equals baseline, ON checkpoint line shows `ضغط التنفيذ=N · أثر الحقبة=M` with M ≤ ABDO_EPOCH_TRAIL_CHARS + one kept window, 📐 line and memory (executionReceipts) identical ON vs OFF. SPRINT 2 (turnBudget): unit tests on PROBE numbers (4558/43/4224 → 1433 at discount 0.25, mirroring token-budget.test.ts:86); local run with ABDO_TURN_TOKEN_CAP=1 + scratch ABDO_TOKEN_LEDGER → ⏱ line `فعّال=0/1`, no refusal (local calls are outside the ledger, token-budget.ts:13); plugins.turnBudget=false → no ⏱ line, trail identical to ON-without-env; ABDO_TURN_TOKEN_CAP=abc with a cloud model SELECTED → first call refused naming ABDO_TURN_TOKEN_CAP, `turn_budget` hand-back, ledger file byte-unchanged and zero network (allowed under the freeze: nothing is spent). SPRINT 3 (routerGate): front-gate unit tests; routerGate:'cheap' with local ollama: «مرحبا» → `🚪 البوابة: أُجيب`, zero tool frames, done completed; the agent task → `🚪 … تُركت (lane-agent)` then normal frames; «ماذا فعلت؟» → answered with every path grounded in prior events or escalated with a named reason; «اكمل» → resumeAnnouncement names the real goal not «مرحبا»; routerGate:'off' → no 🚪 payload and stream equals baseline; record the escalation rate. Also assert the turn meter is charged by the gate call (unit: gateAsk with a fake cloud provider increments TurnSpendMeter.calls). === OWNER GATE: only after sprints 1–3 are green with receipts recorded may one paid ON round + one trailCompaction-OFF control on the same objective be approved (program gate PRODUCT-CLOSURE-10-SPRINT-PROGRAM.md:283-284, compared by 💳 فعّال). === SPRINT 4 (registry): plugin-registry unit tests (defaults equal reader fallbacks — pinned by a source scan; env pin wins only when settingsSeam on; malformed pin refused by name; validatePluginsPatch refusals; descriptor-count-driven, no hardcoded 11/12); framed-stdio child with isolated USERPROFILE/HOME: ready carries the registry field with revision 0; unknown plugin refused; non-boolean refused; fenced write bumps revision; stale fence → refused + fresh settings frame; unfenced write accepted; `pluginsRevision` in a patch refused as unknown field; settingsSeam=false → fence ignored, no bump; inventory OFF → field absent (not []); then one local turn whose `plugins` frame lists swarm/reviewer/activity with reads:0 and the desktop panel (bun run --cwd packages/desktop prepare) shows rows only after ready, two windows saving → second refused and re-synced; baseline diff of model-facing input byte-identical. SPRINT 5 (receiptFixtures): replay every test/fixtures/receipts-*.jsonl through the detectors (one test per record), coverage test over all PLAYBOOKS ids, loader fail-closed cases, tap/capture/redaction on mkdtemp dirs incl. post-redaction SECRETISH refusal, frame-log fallback marks truncated; `bun src/cli.ts fixture verify` prints ✓ per file; one local failing-`npm test` turn writes <STATE_ROOT>/receipt-tap/<turnId>.jsonl (≤400 lines, redacted) and `fixture capture` promotes one record; toggle OFF → no directory created and `fixture` refuses by name; .gitignore covers the tap dir (`git status` clean after a run). SPRINT 6 (intentField): loop tests (split/forward/OFF-identity/write-payload-never-split/native intent never enters command), native-agent-tools mode-aware schema tests, distiller/ledger tests (≤25 words, key from normalizeErrorSignature, REASONS-only vocabulary, dedupe, cap 6, resolved_error); local qwen text-mode + native-mode turns ON against the failing `npm test` project: 🎯 line per epoch, ≥1 `lesson:` fact in abdocode-memory.sqlite, next turn's recall brief contains it, 📐 line and receipt bytes identical to the OFF control; keep default OFF until stated-intent compliance ≥90% on the local lane. SPRINT 7 (UI trio): pure reducer tests for approval/trajectory/deliverables; approval-ledger + tool-locations tests (SERVED_URL_RE identity with turn-memory's export); serve-wiring pins incl. import-order (asked line before approval frame; decided line before resolve; `tap`-style `?.` only) and generated-js presence; desktop against the local 9B in read-only mode: write → diff block + composer takeover, Esc → denied with 🔐 asked/decided pair in the journal and the model receipt unchanged; approve → decided seq < tool-result seq; interrupt during pending approval → أُلغي بالمقاطعة and turn ends; Trajectory tab shows 📐/💳 under the right epoch; deliverables shows only the verdict-ok write and the managed-server URL flipping to أُوقف at done; each toggle OFF → its .js not requested (Tauri devtools network) and legacy rendering returns.

## التقدير
≈ 9–10 focused implementer-days serial (one implementer, one worktree at a time because of the shared cli.ts/index.html seams): cost trio ≈ 2 days (S13.0-b 4–6h, turnBudget 4–6h, routerGate 5–7h) — this is the gate for the next paid round; registry sprint (5+12 merged, WP1+WP2, boolean-only; DSL WP-B excluded) ≈ 1.5 days; receiptFixtures ≈ 1 day; intentField ≈ 1 day; UI trio ≈ 3 days; plus ≈ 0.5–1 day of rebase/re-pin overhead on the shared seams (index.html:1341 literal, serve-wiring/token-economy pins, cli.ts:889, onToolResult closure). Optional add-ons not counted: IDEA 11 `{when}` DSL (≈ 0.5 day after registry, owner decision), thin maxRounds 32→16 (owner decision, recommended NOT before measuring compaction), and the paid ON/OFF live receipt after sprint 3 (separate owner-approved cost).

## السبائك (ملخّص؛ النصّ الكامل لكل سبيكة في scratchpad/spec-*.json وقت الجلسة)

### S13.0-b — generic prefix-preserving TRAIL compaction: extend the existing read-digest pass (plugins.readCompaction) with an execution-class 
- الأولوية: cost · المفتاح: plugins.trailCompaction — default ON (checked in desktop row + reset patch, `!== false` read once per turn like readCompaction at cli.ts:2618 and resumeIntent :2381 — flip applies from the next turn, 
- التقدير: One focused session ≈ 4–6 h: text-agent-loop.ts ≈ 120–160 changed/added lines; index.ts 5; cli.ts ≈ 15; index.html 2; engine-host tests ≈ 150 lines (8 tests + replay dry-run); engine wiring tests ≈ 30
- الملفات:
  - packages\engine-host\src\text-agent-loop.ts (LF, 492 lines) — options :57 add trailCompaction; ReadTrailEntry :60 → TrailEntry (+kind, verdictLine), keep alias; READ_DIGEST :66 + new EXE
  - packages\engine-host\src\index.ts (CRLF) — :148-160 export compactTrail, isExecDigest, isTrailDigest, type TrailEntry (keep compactReadTrail, isReadDigest, ReadTrailEntry)
  - packages\engine\src\cli.ts (CRLF, 3329 lines) — :388 add EPOCH_TRAIL_CHARS (env ABDO_EPOCH_TRAIL_CHARS, bounded like AGENT_CONTEXT_TOKENS :551-555) and TRAIL_COMPACTION; after :2618 add 
  - packages\desktop\ui\index.html (CRLF) — after :320 new setting-row `data-plugin="trailCompaction"` checked (label: ضغط إيصالات التنفيذ — إيصالات run/write/edit القديمة تُطوى إلى سطر الحك
  - packages\engine-host\test\text-agent-loop.test.ts (CRLF) — new describe "prefix-preserving trail compaction (exec class)" reusing runReads/prefixBreaks helpers :6-39; extend purity test 
  - packages\engine\test\token-economy-wiring.test.ts (LF) — new wiring pins for the trailCompaction spread, checkpoint text, TRAIL_COMPACTION shape/validity, ABDO_EPOCH_TRAIL_CHARS bounds
  - packages\engine\test\serve-wiring.test.ts (LF) — :93-95 add `data-plugin="trailCompaction"` and `trailCompaction: true,` pins
  - packages\engine\src\rail-policy.ts (LF) :45 and packages\engine\test\rail-policy.test.ts :46 — ONLY if the owner decides 32→16 (not part of this sprint)
- الاختبارات التي تسقط إن لم يُوصل:
  - engine-host (g) exec digests: 6 `run` commands whose dispatch returns DispatchResult { output: `$ cmd\n` + 3000 chars + `\nانتهى الأمر برمز 1`, verdict: { ok:false, reason:"nonzero_exit", denied:false
  - engine-host (h) OFF byte-identity: same fixture with readCompaction { keepRecent: 4, overChars: 60_000 } and NO trailCompaction → continuation deepEquals the run at d1209e4 semantics (i.e. equals a ru
  - engine-host (i) trailChars trigger: 12 runs × 700-char outputs (each far under overChars 30_000) with trailChars 5000 → exactly one pass fires at the first ask where the epoch trail exceeds 5000 AND c
  - engine-host (j) compactTrail purity: mixed entries [read, exec, read, exec] with keepRecent { read: 1, exec: 1 } → compacted { read: 1, exec: 1 }, input trail untouched, roles/toolCallId/name intact, 
  - engine-host (k) native mode: NativeAgentReply run calls (kind exec) produce tool-role observations that are digested keeping toolCallId and name (mirror of test (d) :155-181); the 4th ask already sees
- المخاطر:
  - Exec bodies are not recoverable in-epoch: unlike reads (knownReads replay :271, :421-427), a repeated identical run is a `duplicate` stop (:419-430) unless the workspace generation moved. A model repa
  - Write payloads are the real write-side mass and are out of scope: the file content lives in the model's own assistant turn (`recordAssistant` :300-302), the write receipt is ~1-3 lines (:1507). Compac
  - Prefix-cache breaks: every pass rewrites already-sent positions (94% cache hit tonight is the asset). Bounded by the overChars crossing rule and the quarter guard on the trailChars trigger; the tests 
  - Cross-epoch behaviour change: `continuation` shrinks, so `trimEpochHistory` (epoch-context.ts:33, 48KB, keepRecent 8) keeps MORE older messages across epochs — more digests survive, fewer full receipt

### IDEA 10 — per-TURN effective-token cap with one close_to_done grace (anton `session.py` `_spend_ceiling_gate`/`_grant_spend_ceiling_grace` +
- الأولوية: cost · المفتاح: `plugins.turnBudget` — read once per turn via `loadSettings().plugins?.turnBudget !== false` (same idiom as cacheAccounting at cli.ts:889, walls at 2502) so a flip takes effect next turn, never mid-tu
- التقدير: One focused implementation session, ~4–6 h: turn-budget.ts (~120 lines) + turn-budget.test.ts (~150 lines) ≈ 1.5 h; cli.ts nine edit sites (~40 lines, CRLF, Edit tool) ≈ 1 h; sprint-guard export + tes
- الملفات:
  - packages\engine\src\turn-budget.ts  (NEW, LF — sibling of token-budget.ts; exports turnTokenCap(), TurnSpendMeter, closeToDone(), renderTurnBudgetLine(); imports effectiveTokens/cacheDis
  - packages\engine\src\cli.ts  (CRLF 3329/3329 — measured by byte count; `file` also says CRLF; awk/grep in Git Bash strip CR and lie) — sites: import line 61 (token-budget import) + line 7
  - packages\engine\src\project-sprint-plan-guard.ts  (CRLF 130/130) — extract the section splitter (line 122 `text.split(/^(?:#{1,6}\s+)?(?=(?:ال)?سبرنت\s+\d+|sprint\s+\d+)/gimu)`) and stat
  - packages\engine\test\turn-budget.test.ts  (NEW, LF — same helpers style as token-budget.test.ts:1-13, PROBE numbers 4558/43/4224 → 1433 as token-budget.test.ts:86)
  - packages\engine\test\token-economy-wiring.test.ts  (LF) — line 13 exact-string assertion MUST change to the `const charged = chargeableUsage(...)` shape (+ `source.match(/chargeableUsage
  - packages\engine\test\serve-wiring.test.ts  (LF) — lines 95 and 205 pin `"...cacheAccounting: true, resumeIntent: true }"`; both must become `"cacheAccounting: true, resumeIntent: true, t
  - packages\engine\test\project-sprint-plan-guard.test.ts  (LF) — add openSprintCount cases on the `completePlan` fixture (line 8)
  - packages\desktop\ui\index.html  (CRLF 1514/1514) — new `.setting-row` after the resumeIntent row (line 322) with `data-plugin="turnBudget" checked` and effect text «يسري من الدور القادم»
- الاختبارات التي تسقط إن لم يُوصل:
  - engine/test/turn-budget.test.ts (NEW): turnTokenCap — undefined for undefined/""/"  "; "invalid" for "abc", "0", "-5", "1e6", "12.5", "9007199254740993"; 600000 for "600000" and " 300000 ".
  - turn-budget.test.ts: charge uses effectiveTokens — PROBE entry {inputTokens:4558, outputTokens:43, cachedInputTokens:4224} at discount 0.25 → spent 1433 (mirror token-budget.test.ts:86); an entry with
  - turn-budget.test.ts: verdict — cap 2000, spent 1433, est 600 → allowed:false, message contains "ABDO_TURN_TOKEN_CAP" and "منفَق 1433 + مقدَّر 600 > سقف الدور 2000", snapshot().tripped true, refusals 1
  - turn-budget.test.ts: gate — cap 10000, one call of 4000 → open (4000+4000 ≤ 10000); second call 4000 → exhausted (8000+4000 > 10000); grantGrace(3) returns min(ceil(10000/4)=2500, 2*4000=8000)=2500 an
  - turn-budget.test.ts: closeToDone truth table — {probePending:true, openSprints:5, receipts:3} → true; {false, 1, 3} → true; {false, 2, 3} → false; {false, undefined, 3} → false; {true, 1, 0} → false (
- المخاطر:
  - LIVE TREE FROZEN: a paid round is running from <جذر المستودع> right now — implement in a separate worktree and land only after it ends; do not run bun/cargo in this tree.
  - Refusal-as-prose: text-agent-loop.ts:356-379 treats the `ask` refusal string as a model reply (stopReason "complete", zero commands). If the `tripped` block is placed after line 2704 or forgotten, the
  - Estimate error: the pre-check bounds by `estimated + requestOutputCap` (cli.ts:818, ARABIC_UNDERCOUNT heuristic at 771). The last admitted call can land over the cap by its own estimation error (tonig
  - Grace predictor is `peakCall`; a next call larger than any previous one can hard-trip mid-grace → honest hand-back with no second grace (by design). Anton's known overshoot (up to 1.5× ceiling) cannot

### IDEA 4 — بوابة أمامية رخيصة (anton thalamus): a toolless front model answers trivial chat-lane turns (greetings, questions about the convers
- الأولوية: cost · المفتاح: Setting `routerGate` in ~/.config/abdocode/settings.json (flat key beside `railPolicy`, validated in validateSettingsPatch; NOT under `plugins` because it is tri-state and the desktop serialises `
- التقدير: One focused implementation session (≈5-7 h): front-gate.ts ~180 lines + front-gate.test.ts ~160 lines (2.5 h); cli.ts ~75 lines across four spots in CRLF (1.5 h); resume-intent.ts 4 lines + tests (0.5
- الملفات:
  - packages/engine/src/front-gate.ts (NEW, LF): parseGateMode, gateEligibility, condenseForGate, buildGateSystem, interpretGateTurn, gateEventLine, GATE_OUTPUT_TOKENS=512, ESCALATE_SENTINEL='[[ESCALATE]]'; imports only Mode
  - packages/engine/src/cli.ts (CRLF): Settings type :596-611 (+routerGate, +gateModel); SETTINGS_KEYS :614; validateSettingsPatch ref loop :643 (+"gateModel") and enum check beside :654; new `gateAsk` placed after `selectTu
  - packages/engine/src/resume-intent.ts (LF): pickPriorGoal :41-58 skip status "answered"; STATUS_LABELS :60-64 (+answered)
  - packages/desktop/ui/index.html (CRLF): plugins panel :310-322 (+select row id=setroutergate, options off/cheap/auto); fillSettingsControls :1192 (+routerGate); save patch :1310 (+routerGate); reset patch :1341 (+routerGa
  - packages/engine/test/front-gate.test.ts (NEW, LF)
  - packages/engine/test/serve-wiring.test.ts (LF): new test block for routerGate wiring + desktop rows
  - packages/engine/test/token-economy-wiring.test.ts (LF): :12-17 honest update — recordCloudUsage({ count 1→2, second site must spread chargeableUsage(decoded.usage, estimated, GATE_OUTPUT_TOKENS, ...) and carry note: "rou
  - packages/engine/test/resume-intent.test.ts (LF): pickPriorGoal skips answered facts; label
- الاختبارات التي تسقط إن لم يُوصل:
  - packages/engine/test/front-gate.test.ts (NEW): parseGateMode('off'|undefined|'yes'|true) → 'off'; 'cheap'/'auto' pass through — FAILS if module missing
  - front-gate.test.ts: gateEligibility — lane 'agent' → lane-agent (uses classifyModelLane('اصلح ملف cli.ts واختبر الحزمة') from @abdo/providers to prove the deterministic router is upstream); requireSpr
  - front-gate.test.ts: condenseForGate — toolCalls collapse to '[ran tool: run]' and 9000-char content is not leaked; same-role merge; per-entry cap holds after merge (marker counted); window keeps last 
  - front-gate.test.ts: buildGateSystem contains '[[ESCALATE]]', does not contain '{{tool-catalogue}}' nor 'نفّذ:', embeds the recall block under 'حقائق موثَّقة' only when non-empty
  - front-gate.test.ts: interpretGateTurn — kind 'tools' → gate_tool_call; truncated:true → gate_output_overflow; '<think>x</think>' only → gate_empty; '[[ESCALATE]]\nيحتاج قراءة ملف' → gate_delegated wit
- المخاطر:
  - A confident wrong direct answer in prose that carries no path and no effect verb slips past the deterministic guards (e.g. 'المشروع مكتمل'); mitigated by the answered line being labelled بلا أدوات, th
  - Double cost on ambiguous chat-lane turns that escalate: gate prompt (~1.5-3k in, ≤512 out) is spent and then the loop runs as before; bounded by the deterministic eligibility filter and by `auto` skip
  - Escalation does not upgrade the lane: a turn the gate judges 'needs tools' still runs on the chat model through the loop exactly as today; a lane upgrade (chat→agent on gate_delegated) is deliberately
  - token-economy-wiring.test.ts:16 pins exactly one `recordCloudUsage({` in cli.ts; the honest fix is to raise the pin to 2 and pin the gate site's chargeableUsage spread — do not dodge the literal

### IDEA 2 — plugins.intentField (anton cerebellum, ideas-not-fork): a mandatory one-line intent per tool call stated by the model INSIDE the sa
- الأولوية: quality · المفتاح: plugins.intentField — read once per turn at turn start via `loadSettings().plugins?.intentField === true` (cli.ts Settings.plugins: Record<string, boolean> at 607; same `=== true` default-OFF idiom as
- التقدير: ~1 focused day: engine-host intent-line.ts + loop threading (2h), native-agent-tools mode-aware schema/validator/prompt (1.5h), engine intent-field.ts ledger+distiller (2h), cli.ts wiring + desktop ro
- الملفات:
  - packages/engine-host/src/intent-line.ts (NEW, LF) — INTENT_PREFIX = "— النية:"; splitIntent(text): { intent?: string; body: string } — extracts ONLY a leading line (after the same stripMeasure filter as text-agent-loop.t
  - packages/engine-host/src/text-agent-loop.ts (w/lf) — TextAgentLoopOptions (lines 27-58): add `readonly intentField?: true` (absent = legacy, same idiom as readCompaction:57); onTool/onToolResult signatures (33-34) gain t
  - packages/engine/src/native-agent-tools.ts (w/lf) — nativeToolDefinition(tool, intentField = false) (12-19): when true append `intent` to `required` and `properties` ({type:"string", description:"سطر نية واحد: ماذا تتوقع 
  - packages/engine/src/intent-field.ts (NEW, LF) — imports verdictIsBreakage/REASONS/ToolVerdict from @abdo/engine-host, classifyFailureTier/normalizeErrorSignature/receiptFailed from ./failure-tiering (47, 58, 99), Distill
  - packages/engine/src/cli.ts (w/crlf — edit with CRLF-preserving Edit, never rewrite) — AskHooks (563-566): add `readonly intentField?: true`; ask(): nativeToolDefinition(tool, hooks.intentField === true) at 737, nativeAge
  - packages/desktop/ui/index.html (w/crlf) — plugins panel (310-322): new row `<div class="setting-row"><label>حقل النيّة ودروس المخالفة<small>سطر نية لكل أداة؛ عند الفشل يُقطَّر فرق النية والواقع درساً دائماً ≤25 كلمة بلا 
  - packages/engine/test/serve-wiring.test.ts (w/lf) — update the pinned defaults substring at line 95 to `miner: true, readCompaction: true, cacheAccounting: true, resumeIntent: true, intentField: false }`; update the pinne
  - packages/engine/test/intent-field.test.ts (NEW, LF)
- الاختبارات التي تسقط إن لم يُوصل:
  - engine-host/test/text-agent-loop.test.ts — «with intentField the leading — النية line is split off, the command dispatches unchanged, and intent reaches onTool/onToolResult»: scripted ask returns `— ا
  - engine-host/test/text-agent-loop.test.ts — «(b) without intentField the same reply is the legacy invalid proposal and the follow-up prompt is byte-identical»: run the loop twice (option absent) on a r
  - engine-host/test/text-agent-loop.test.ts — «a write payload is never split»: `— النية: أكتب الملف\nنفّذ: write a.txt <<<\n— النية: not an intent\nx` → dispatched command payload contains the second li
  - engine-host/test/text-agent-loop.test.ts — «native intent comes from call.input.intent and never enters the command»: NativeAgentReply with input {command:"npm test", intent:"أشغّل الاختبارات"} → disp
  - engine/test/native-agent-tools.test.ts — «intentField=true makes intent required in every schema and validates it»: nativeToolDefinition(tool, true).parameters.required includes "intent" for all 7 too
- المخاطر:
  - Compliance cost on small local models: an extra required line in text mode may be omitted or mangled; the spec deliberately never rejects in text mode (counted, not blocking) so progress is unaffected
  - Cache prefix: the system-prompt sentence changes the cached prefix at the moment the toggle flips (one-time break, then stable since the toggle is read once per turn); the intent line itself adds ~20 
  - Lesson quality is template-shaped, not LLM-written: a deterministic distiller cannot judge generalizability; the normalized signature generalizes the key, the value still carries the model's verbatim 
  - Pinned source tests must be updated in lockstep (serve-wiring.test.ts:95 defaults literal; the 5-arg onToolResult pin; token-economy-wiring's single-runTextAgentLoop pin) — forgetting one turns the ga

### IDEA 3 remainder (anton ACC habit, layer-3 replay): every receipt-string detector and every playbook ships with its incident trace as a repl
- الأولوية: quality · المفتاح: plugins.receiptFixtures — default ON (mirrors walls/miner/toolVerdict), read once per turn via `loadSettings().plugins?.receiptFixtures !== false` at the same site as the other toggles (cli.ts:2500-25
- التقدير: One sprint, ~6–8 focused hours after WP0 clears: receipt-fixtures.ts ~220 lines; redactSecretValues ~30; cli.ts ~45 lines across 5 edit sites (CRLF); desktop 2 lines; .gitignore 1; tests ~260 lines pl
- الملفات:
  - packages\engine\src\receipt-fixtures.ts (NEW, LF) — ReceiptFixture type, FIXTURE_FAMILIES, parseFixtureJsonl(text,file), expectationsFor(record) and evaluateFixture(record) over classify
  - packages\engine\src\secret-command-guard.ts (LF) — export `redactSecretValues(text): { text: string; redactions: number }` built on the module-private looksLikeSecretValue (:10-19) and S
  - packages\engine\src\cli.ts (CRLF) — import; `const fixturesOn = loadSettings().plugins?.receiptFixtures !== false` + `const tap = fixturesOn ? openReceiptTap(STATE_ROOT, turn.id) : undef
  - packages\engine\test\receipt-fixtures.test.ts (NEW, LF) — replay suite, coverage test, loader fail-closed tests, tap/capture/frame-log unit tests on mkdtemp dirs, static wiring pins agai
  - packages\engine\test\fixtures\receipts-playbooks.jsonl (NEW, LF) — 31 records migrated from error-playbooks.test.ts:5-37 table (source:"synthetic" unless a live trace is known) + live tr
  - packages\engine\test\fixtures\receipts-walls.jsonl, receipts-miner.jsonl, receipts-tiering.jsonl, receipts-facts.jsonl, receipts-acceptance.jsonl, receipts-adapter.jsonl (NEW, LF) — seed
  - packages\engine\test\serve-wiring.test.ts (LF) — the default-map tail pins at :95 and :205 (`cacheAccounting: true, resumeIntent: true }`) must become `..., resumeIntent: true, receiptFi
  - packages\engine\test\secret-command-guard.test.ts (measure EOL) — redactSecretValues cases
- الاختبارات التي تسقط إن لم يُوصل:
  - receipt-fixtures.test.ts — replay: for every test/fixtures/receipts-*.jsonl, one `test("<file>:<id>")` per record asserting evaluateFixture(record).ok with diffs in the failure message; FAILS if a det
  - receipt-fixtures.test.ts — coverage (anton layer-4 analog): every id in PLAYBOOKS (error-playbooks.ts:21) appears in ≥1 record's expect.playbooks across all fixture files; FAILS today and drives the 3
  - receipt-fixtures.test.ts — loader fail-closed: a line with unknown family / unknown verdict.reason / missing output / duplicate id throws FixtureFormatError naming file and 1-based line; CRLF-terminat
  - receipt-fixtures.test.ts — tap unit: openReceiptTap(tmp, "t1").observe("run npm test", out, verdict, 2) creates receipt-tap/t1.jsonl with one redacted line; observe("read a.ts", …) and observe("write 
  - receipt-fixtures.test.ts — capture from tap: a tap line whose output contains `TOKEN=sk-live_ABCDEFGHIJKLMNOP1234` → the appended fixture contains «مُحجَّب», never `sk-live`, redactions ≥ 1, source "t
- المخاطر:
  - Secrets on disk: the tap persists full run receipts under STATE_ROOT (defaults to packages/engine/src when ABDO_CODE_STATE_DIR is unset, cli.ts:105-106) — mitigated by run-only capture, redaction a
  - Frozen wrong behaviour: `fixture capture` records `expect` from the CURRENT detectors, so a mis-classifying detector would be enshrined as expected — mitigated by printing the computed expect for supe
  - Frame-log fallback is lossy by construction (500/1500-char head slices lose the trailing exit marker); such records are labelled truncated and their expectations rest on the verdict field — a run capt
  - Two serve-wiring pins (:95, :205) hard-code the desktop default-map tail; forgetting them makes the whole engine suite red on the desktop edit — listed as a required edit, not a side effect

### IDEA 5 — settings namespaces with per-plugin schemas (dsh settings seam): a single plugin-descriptor registry in the engine drives (a) fail-
- الأولوية: ui · المفتاح: plugins.settingsSeam — default ON (`!== false` idiom like walls/miner/toolVerdict). Governs the two NEW runtime behaviours only: (1) the revision fence (`expectedPluginsRevision` honoured; `pluginsRev
- التقدير: WP1 (registry + validation + fence + contract + generated panel + T1–T4): one focused session, ~4–5 hours including the dry-run in §8 — ~120 lines new module, ~45 lines cli.ts delta, ~60 lines index.h
- الملفات:
  - packages/engine/src/plugin-registry.ts (NEW, LF): PluginName, PluginDescriptor, PLUGINS, envPinName, resolvePlugins, validatePluginsPatch, describePlugins, PLUGINS_UNKNOWN/PLUGINS_NOT_BOOLEAN refusal builders
  - packages/engine/src/cli.ts (CRLF): type Settings :596-612 (+ pluginsRevision?: number, NOT added to SETTINGS_KEYS :614); saveSettings :624-633 (revision bump when 'plugins' in patch, gated by settingsSeam); validateSetti
  - packages/transport-contracts/src/shell-protocol.ts (LF): inbound('settings-set', …) :41 allowed += 'expectedPluginsRevision'; validateShellFrame settings-set branch :112-113 bounds it (safe non-negative integer)
  - packages/desktop/ui/index.html (CRLF): plugins section :310-323 (delete static rows :313-322, add <div id=pluginrows> + offline note); runtimeSettings block :1155-1161 (+ let pluginRegistry = null); ready handler :824-83
  - packages/engine/test/plugin-registry.test.ts (NEW, LF)
  - packages/engine/test/serve-wiring.test.ts (LF): :95-97 three assertions on static rows / reset literal must be replaced; + new test 'plugin registry drives validation, the panel and the fence'
  - packages/engine/test/framed-stdio.test.ts (LF): after :89 add the unknown-plugin refusal, the conflict round-trip, and the ready-frame registry assertions
  - packages/transport-contracts/test/transport-contracts.test.ts (LF): beside :43 add expectedPluginsRevision accept/reject cases
- الاختبارات التي تسقط إن لم يُوصل:
  - T1 packages/engine/test/plugin-registry.test.ts (NEW, LF), pure unit: (a) resolvePlugins(undefined, {}) equals every descriptor's defaultOn; (b) file value overrides default only for registered names 
  - T2 packages/engine/test/serve-wiring.test.ts (LF) — replace :95-97 and add one test reading cli.ts, index.html, shell-protocol.ts, plugin-registry.ts as text: (a) cli.ts contains `import { PLUGINS, de
  - T3 packages/engine/test/framed-stdio.test.ts (LF) — extend the existing carrier test after :89 (same child, same isolated `USERPROFILE`/`HOME`): (a) the buffered `ready` frame has `pluginRegistry.desc
  - T4 packages/transport-contracts/test/transport-contracts.test.ts (LF) beside :43: `validateShellFrame({ kind: "settings-set", settings: {}, expectedPluginsRevision: 3 }).ok === true`; `expectedPlugins
  - T5 (WP2 only) token-economy-wiring.test.ts :13 → `expect(source).toContain("...chargeableUsage(decoded.usage, estimated, requestOutputCap, pluginOn(\"cacheAccounting\")),")`; :27 → `...(pluginOn("read
- المخاطر:
  - Live paid round in this tree: any edit, `bun test`, or cargo before it ends corrupts the round's measurement. Gate: e2e `done` frame + no process with this cwd (WP0 of the tool-verdict spec applies ve
  - Existing tests fail by construction the moment rows are generated: serve-wiring.test.ts:95-97 assert `data-plugin="readCompaction"`, `data-plugin="cacheAccounting"` and the reset literal — must be rep
  - Contract-first ordering: `Protocol.validate` (cli.ts:2051 → shell-protocol.ts:105-108) refuses `expectedPluginsRevision` as an unknown field until shell-protocol.ts:41 is edited; a UI built ahead of t
  - Optimistic fence, not a lock: two engines sharing `~/.config/abdocode/settings.json` can still interleave read-modify-write inside `saveSettings` (cli.ts:625-631 has no file lock); the fence catch

### Ideas 12 + 11 (dsh): live plugin inventory in the panel («فعّلتُه» ≠ «حُمّل») + conditional toggles via a tiny portable DSL evaluated in TS 
- الأولوية: ui · المفتاح: Two meta-toggles, both stored in the existing `settings.plugins` map (no new Settings key; SETTINGS_KEYS :614 unchanged), both BOOLEAN-ONLY (a `{when}` rule on either is refused at the door with «plug
- التقدير: ~1 working day for one implementer: registry module + unit tests 3h; cli.ts wiring (7 read sites, door validation, 3 frame sites, 2 emit sites) 2h; index.html badges/rule inputs/handler 2h; pin update
- الملفات:
  - packages\engine\src\plugin-registry.ts  (NEW, LF) — PLUGIN_TOGGLES, PluginRule, parseRule, evaluateRule, resolvePlugin, PluginInventory, pluginContext, catalogFor
  - packages\engine\test\plugin-registry.test.ts  (NEW, LF)
  - packages\engine\src\cli.ts  (CRLF) — import block :86-96; `type Settings.plugins` :607; SETTINGS_KEYS :614 unchanged; validateSettingsPatch :645-668 (add plugins shape/rule validation); 
  - packages\transport-contracts\src\shell-protocol.ts  (LF) — SHELL_FRAMES out rows :46-79; add `{ kind: "plugins", dir: "out", required: ["turnId", "entries"] }` next to `settings` :69
  - packages\transport-codegen\test\transport-codegen.test.ts  (LF) — add `expect(one).toContain('kind: "plugins"')` :3-8
  - packages\desktop\ui\index.html  (CRLF) — plugins settings panel :310-323 (state badge + rule input per row); `ready` handler :824-835; `settings` handler :917-921; new `f.kind === "plugi
  - packages\engine\test\serve-wiring.test.ts  (LF) — update pins :107 and :173; keep :95; add registry↔cli↔panel wiring test
  - packages\engine\test\token-economy-wiring.test.ts  (LF) — update pins :13 and :27
- الاختبارات التي تسقط إن لم يُوصل:
  - NEW packages/engine/test/plugin-registry.test.ts (LF): (a) parse table — accepts `rail == thin`, `os != windows && lane == agent`, `rail == strict || rail == medium`; refuses unknown key, unknown valu
  - UPDATE packages/engine/test/serve-wiring.test.ts (LF): :107 → `expect(source).toContain('plugins.read("toolVerdict", "turn")')`; :173 → `expect(source).toContain('plugins.read("resumeIntent", "turn")'
  - UPDATE packages/engine/test/token-economy-wiring.test.ts (LF): :13 → pin `currentPlugins?.read("cacheAccounting", "call") ?? resolvePlugin(loadSettings().plugins, "cacheAccounting"` ; :27 → pin `...(p
  - EXTEND packages/engine/test/framed-stdio.test.ts (LF), after :89, in the live child (ABDO_CODE_SETTINGS-isolated): (1) send settings-set `{ plugins: { verifier: { when: "rail == thin" }, walls: tru
  - UPDATE packages/transport-codegen/test/transport-codegen.test.ts (LF): add `expect(one).toContain('kind: "plugins"')` — fails until the catalogue row exists.
- المخاطر:
  - Source-pin tests break by design: serve-wiring :95/:107/:173 and token-economy-wiring :13/:27 assert the old `loadSettings().plugins?.…` strings; they must be re-pinned in the same commit or the engin
  - Behaviour change for malformed hand-edited settings.json: today any non-`false` value (e.g. a string) reads as ON (`!== false`); after this, a non-boolean/invalid value is refused at the door and reso
  - `plugins` is shallow-replaced on save (`saveSettings` :625 spreads only top-level keys): a partial patch `{ plugins: { walls: false } }` drops every sibling → defaults. Existing behaviour; the panel m
  - `currentPlugins` is module-level state (pattern of `currentRails` :1836). The engine runs one turn at a time (`running` :2374), but the cacheAccounting read at :889 can fire outside a turn (chat/plan 

### Ideas 7+8+9 (dsh ui-approval / ui-trajectory / ui-deliverables): approval as a composer takeover with a durable asked/decided pair in the se
- الأولوية: ui · المفتاح: Three toggles, all in the existing `plugins: Record<string, boolean>` (cli.ts:607; validated by validateSettingsPatch cli.ts:635 which accepts any plugin key; persisted via settings-set cli.ts:2222-22
- التقدير: ~3 implementer-days: 0.5d engine (approval-ledger.ts, tool-locations.ts, turn-memory exports, cli.ts edits at 1155/1165-1172/2264-2270/2302-2306/2403/2560/2646), 1.5d shell (three pure modules + tests
- الملفات:
  - packages\engine\src\cli.ts
  - packages\engine\src\approval-ledger.ts (new)
  - packages\engine\src\tool-locations.ts (new)
  - packages\engine\src\turn-memory.ts
  - packages\engine\src\shells\approval.ts (new)
  - packages\engine\src\shells\trajectory.ts (new)
  - packages\engine\src\shells\deliverables.ts (new)
  - packages\desktop\ui\index.html
- الاختبارات التي تسقط إن لم يُوصل:
  - engine/test/approval-ledger.test.ts (new, behavioural): approvalAskedLine/approvalDecidedLine exact strings for approve/deny/interrupted; APPROVAL_DECIDED_PREFIX is a prefix of every decided line and 
  - engine/test/serve-wiring.test.ts — idea 7 pins that FAIL when unwired: source contains `import { approvalAskedLine, approvalDecidedLine } from "./approval-ledger"`; `plugins?.approvalTakeover !== fals
  - engine/test/shell-approval.test.ts (new, behavioural over the pure reducer): idle + approval frame → asked with the request/class/mode; diff frame before approval sets diffSeen=true for that turn; cho
  - engine/test/tool-locations.test.ts (new): write ok → [{file, op:write}]; edit ok → op:edit; output starting `⏭ ` with ok verdict → op:skipped; verdict undefined → [] even for a successful-looking writ
  - engine/test/serve-wiring.test.ts — idea 9 pins: `const deliverablesOn = loadSettings().plugins?.deliverables !== false`; both tool-result emits contain `...(deliverablesOn ? { locations: locationsFrom
- المخاطر:
  - Journal ordering/latency: gate() now awaits emitEvent before emitting the approval frame; a SQLite append failure would previously not exist on this path. Mitigation in the spec: try/catch around the 
  - Interrupt-resolves-approval is gated behind plugins.approvalTakeover to keep OFF byte-identical, which means the pre-existing dangling-Promise leak on interrupt remains when the toggle is OFF. If the 
  - Event lines reach the assistant block's `lines` and, for non-streamed answers, `finish()` (index.html:632-641) rewrites the body from `lines` — the 🔐 lines will appear there exactly like 📐/💳 do today.
  - `plugins.activity` was declared (index.html:315) but never applied; wiring it now changes behaviour for anyone who unchecked it earlier — their pane will disappear on next start. It is the honest fix 
