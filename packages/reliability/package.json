{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/reliability",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode tool reliability: atomic writes, restore-gated git, package-manager policy, redaction, bounded search, and owned process cleanup.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
