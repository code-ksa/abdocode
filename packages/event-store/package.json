{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/event-store",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode append-only event store with atomic sequences, single-writer serialization, idempotency, and replay.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
