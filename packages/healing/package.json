{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/healing",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode self-healing with stable failure fingerprints, bounded retries, reboot reconciliation, watchdogs, and safe rollback.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
