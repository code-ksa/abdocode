{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/normalizer",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "CL-04 \u2014 cross-shell command normalization. Pure, no execution, no IO. Stage 1: Bash.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/control-contracts": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}