{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/browser",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode browser control: DOM and accessibility first, semantics before pixels, recorded fallback, isolated sessions, and verified targets.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*",
    "@abdo/tools": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
