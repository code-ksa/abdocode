{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/project-index",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode project index with content hashes and an exact-path, symbol, text, then history retrieval ladder.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
