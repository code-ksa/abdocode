/**
 * @abdo/lsp — code intelligence whose failures are visible.
 *
 * The through-line of this package is one refusal repeated at five levels: a
 * dead server is `unavailable` and never an empty list; a language with no
 * server is `unsupported` and never a silent text search; a file whose
 * diagnostics were never obtained is not clean; a symbol with unexplained text
 * occurrences is not safe to delete; and a truncated impact walk means run
 * everything. Each one costs a little convenience and removes a class of
 * confident wrong answer.
 */
export * from "./client"
export * from "./registry"
export * from "./graph"
export * from "./patch"
export * from "./impact"
