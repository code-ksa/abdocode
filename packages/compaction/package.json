{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/compaction",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode context budgeting and compaction with auditable manifests, provenance, safe pruning, and periodic full rebase.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*",
    "@abdo/event-store": "workspace:*",
    "@abdo/context": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
