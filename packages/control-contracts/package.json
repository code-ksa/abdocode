{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/control-contracts",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "CL-01 — the one control-plane decision contract. Pure types + decision hashing; no policy, no execution, no I/O.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
