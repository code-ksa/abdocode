{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/controlplane",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "AbdoCode control plane with journal-owned state, idempotent tasks, attenuated workers, dependency scheduling, and merge gates.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@abdo/contracts": "workspace:*"
  },
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
