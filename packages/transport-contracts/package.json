{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@abdo/transport-contracts",
  "private": true,
  "type": "module",
  "license": "UNLICENSED",
  "description": "R9 local-only versioned transport frames and fail-closed validation. No HTTP, listener, or network runtime.",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsgo --noEmit",
    "test": "bun test"
  },
  "dependencies": {},
  "devDependencies": {
    "@tsconfig/bun": "catalog:",
    "@types/bun": "catalog:",
    "@typescript/native-preview": "catalog:"
  }
}
